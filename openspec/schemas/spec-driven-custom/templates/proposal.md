## Why

<!-- 1-2 sentences. Active voice. Concrete. What problem does this solve and why now? -->

## What Changes

<!-- Bullet list of changes. Mark breaking changes with **BREAKING**. Be specific. -->

-

## Non-Goals

<!-- What this change explicitly does NOT do. Bound the scope to prevent over-specification.
     At least one item required. Example: "Does not migrate existing data." -->

-

## Capabilities

### New Capabilities

<!-- Capabilities being introduced. Assign CAP-* IDs. Each becomes specs/<name>/spec.md.
     Use kebab-case names (e.g., user-auth, data-export, api-rate-limiting). -->

- `CAP-001: <name>` — <brief description of what this capability covers>

### Modified Capabilities

<!-- Existing capabilities whose REQUIREMENTS are changing (not just implementation).
     Only list here if spec-level behavior changes. Use existing spec names from openspec/specs/.
     Leave empty if no requirement changes. -->

- `CAP-00N: <existing-name>` — <what requirement is changing>

## Impact

<!-- Specific affected code paths, APIs, schemas, dependencies, or systems.
     Name files and services explicitly, not "the backend" or "the system". -->

-

## Mode

<!-- Recommended pipeline mode based on complexity and domain familiarity:
     - deep:      full pipeline (proposal → research → specs → design → tasks → apply)
                  use when: unfamiliar domain, cross-cutting change, >5 files affected
     - discovery: lightweight (proposal → specs → apply), advisory gates
                  use when: exploration/spike, obvious solution, single file -->

Mode: deep
